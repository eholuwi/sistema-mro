# Dependencias de desenvolvimento / testes.
# Instale com: pip install -r requirements.txt -r requirements-dev.txt
#
# Versoes fixadas pelo mesmo motivo do requirements.txt: o gate (`.\verify.ps1` e o CI)
# so e confiavel se todos rodam a mesma ferramenta. Um ruff de versao diferente pode
# reprovar formatacao que outro aprova.
pytest==9.1.1
pytest-cov==7.1.0

# Lint + formatador. Config em ruff.toml (ruleset em rampa: so E9 e F por enquanto).
# `F` inclui F821 (nome indefinido) — o mesmo que a antiga guarda pyflakes garantia,
# so que no repo inteiro em vez de apenas `app.py` + `ui/`.
ruff==0.16.0

# v5.8.0 — so para congelar `deploy/launcher.py` em MRO.exe (scripts/portatil.py).
# NAO entra no requirements.txt: nada muda no que roda no servidor, e o Streamlit
# continua sendo executado pelo Python do `runtime\`, nunca congelado.
pyinstaller==6.21.0
