CHANGELOG — MRO Inventus Power v6.4.0
Data de Lançamento: (pendente)
Tipo de Release: **Minor** — (pendente)
Status: 🔧 Planejada — aguardando implementação pelo Claude

# 🎯 Resumo Executivo

> Esqueleto de changelog. **Implementação pelo Claude** em sessão própria, a partir de
> `docs/prompt.md` (seção "v6.4.0") e `docs/HANDOFF.md`. Preencher conforme o que entrar.

# ✨ O que entrou

## 1. Consumo mensal por vida útil do lote (Épico B)
- Regra: duração = data de chegada → data que bateu o mínimo; `consumo_mensal = qtd ÷ dias × 30`.
- Onde: `services/classificacao.py` + Ficha 360.

## 2. Sugestão de Min/Max (Épico C)
- Fórmula: `min = consumo_diário × lead_time` · `max = consumo_diário × 60d`.
- Colunas novas + migração + "Usar calculado" e visão em lote.

## 3. Gestor: rejeitar + ver requisição completa (Épico D)
- `rejeitar_requisicao(req, gestor, motivo)` + colunas `rejeitado_*` + expander com itens.

## 4. Requisitante: checkbox "mostrar saldo" + imagem na requisição (Épico E)
- `inventario.mostrar_saldo_requisitante` (default 1) + foto via `imagem_path` na requisição.

## 5. Portaria: busca por nome do requisitante (Épico F)
- `buscar_requisicoes_por_emitente` + input por nome.

---

# 🧪 Testes

(por preencher)

---

# 🔄 Compatibilidade

- `mro.db` da v6.3.0 abre na v6.4.0: colunas novas aditivas/nullable com backup antes.
- (por preencher)
