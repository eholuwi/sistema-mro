# Database
mro.db
mro.db.bak-*
# v5.5.0 (F5) — os .bak passaram a ser gravados aqui, ao lado do banco.
backups/
*.db-journal
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# Testes / cobertura
.pytest_cache/
.ruff_cache/
.coverage

# Pacotes de release gerados por scripts/release.py e scripts/portatil.py
dist/

# v5.8.0 — montagem do pacote portátil: runtime embeddable, cache do download,
# saída do PyInstaller. ~440 MB de binário reconstruível a partir do código.
build/
*.spec

# Streamlit
.streamlit/secrets.toml

# Ficha 360 — imagens de produto (dado operacional, fora do versionamento)
docs/itens/

# Graphify — grafo de conhecimento gerado localmente
graphify-out/
graph.json

# ── Dado operacional e material de trabalho — NUNCA versionar ────────────────
# O repositório é PÚBLICO. O que entra aqui fica no histórico para sempre.

# Exports do inventário: part number, estoque, lead time, local, fornecedor e
# criticidade "Parada de Linha" — o mapa de quais materiais param a produção.
Inventário Improdutivo*.xlsx
Inventário Improdutivo*.csv

# Planilha de controle antiga do almoxarifado (118 MB): 1.004 itens com PN,
# descrição, estoque diário e 402 fotos embutidas. É a MESMA informação dos
# exports acima, em outro formato — e o volume ainda deixaria o clone inviável.
# Fonte do `scripts/importar_imagens_planilha.py`; vive ao lado do repo, não dentro.
Material MRO*.xlsx

# Vault Obsidian (segundo cérebro). Foi escrito sob a premissa de que era
# ignorado pelo git; a premissa mudou para sincronizar entre máquinas, sem
# reauditar o conteúdo. Contém colegas nomeados com cargo, valores em R$ reais da
# operação e material de outro setor. Sincroniza pelo OneDrive, como o resto.
vault/

Minhas conversas com o claude
docs/claude/
