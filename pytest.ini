[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    fase1_baseline: Caracteriza o comportamento ATUAL do sistema (pode conter bug conhecido). ATENCAO: testes marcados assim sao TEMPORARIOS e devem ser REMOVIDOS ou INVERTIDOS durante a fase que corrige o debito correspondente (ver reason de cada xfail relacionado).
